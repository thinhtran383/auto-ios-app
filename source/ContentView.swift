import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("BoxAutomation").font(.title).bold()
            Text("Hello iPhone jailbreak!").foregroundColor(.secondary)
        }
        .padding()
    }
}